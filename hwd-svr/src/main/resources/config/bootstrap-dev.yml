debug: true

server:
  port: 20110

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/

logging:
  config: classpath:config/log4j2.xml
  level:
    root: info
    rebue: trace
    com.netflix.discovery: off

spring:
  rabbitmq:
      host: 127.0.0.1
      port: 5672
      username: admin
      password: admin
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hwd?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: root123

appid: 1